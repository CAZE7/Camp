/**
 * WP-4 (#393): ELK-Adapter (elkjs) — globaler Layout-Pass.
 * Optionen aus Tokens (WP-1), Kollisionsbegriffe aus dem Modell (WP-3),
 * Worker-Vertrag P-6 (letzte Anfrage gewinnt), Fallback: bestehender Router.
 */
export * from './graph';
export * from './runner';
